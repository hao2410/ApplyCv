/** @type {import('next').NextConfig} */
const config = {};

export default config;

// /** @type {import('next').NextConfig} */
// const config = {
//     webpack(config, { dev, isServer }) {
//       if (dev) {
//         config.devtool = 'eval-source-map'; // Sử dụng 'eval-source-map' trong môi trường phát triển
//       } else {
//         config.devtool = 'hidden-source-map'; // Sử dụng 'hidden-source-map' trong môi trường sản xuất
//       }
//       return config;
//     },
//   };
  
//   export default config;
  